<Warning>
  Todo valor nesta API é **inteiro em centavos**. R\$ 100,00 se envia como `10000`.
  Um decimal como `100.00` é recusado com `422` — veja [Valores em centavos](/essenciais/valores-em-centavos).
</Warning>
